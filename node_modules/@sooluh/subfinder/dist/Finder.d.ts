import { Callback as callback } from "./interfaces";
declare class Finder {
    private crtsh;
    private status;
    private ips;
    private parser;
    lookup(domain: string, callback?: callback): Promise<any>;
}
export default Finder;
